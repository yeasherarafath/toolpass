/**
 * Tabler core JS (bundles Bootstrap JS: dropdowns, modals, toasts, etc.)
 */
import '@tabler/core/dist/js/tabler.min.js';

/**
 * Application bootstrapping (livewire, alpine, or custom scripts go here).
 */
